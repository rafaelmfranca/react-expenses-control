import styled from 'styled-components';

const StyledAvatar = styled.div`
  position: absolute;
  top: 1.2rem;
  right: 1.2rem;

  img {
    border-radius: 50%;
    width: 2.1rem;
  }
`;

export default StyledAvatar;
